datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model requests {
  name  String @unique
  type String  @default("POST")
  url String 
  body String @default("{}")
}